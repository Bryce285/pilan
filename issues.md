## ISSUES
### Server
* The command from the client is being included as part of the file on upload
* In the socket stream writer / storage manager currently treats file data as char but should be unsigned char / uint8

### Client
* In the socket stream writer / storage manager currently treats file data as char but should be unsigned char / uint8
