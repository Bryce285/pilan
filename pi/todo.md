### Server todo
* Make "server mode" and "local mode" options
* local mode allows local file encryption/decryption
* the networking stuff is not started unless in server mode
* allow users to create a config file to customize default directories
* integrate automatic wifi AP configuration and automatic daemonization as an option (note then when adding daemonization, a "display key" utility should also probably be added)
* add a key reset option that deletes the mdk file
